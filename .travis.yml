language: php

php:
  - 5.4

before_script:
  - pear install phpunit/DbUnit
  - phpenv rehash
  - cd tests

script: phpunit .

notifications:
  webhooks:
    urls:
      - http://www.ilch.de/ilch2/versions/
    on_success: always
    on_failure: never
    on_start: false
